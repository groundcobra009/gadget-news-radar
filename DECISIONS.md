# DECISIONS.md — 確定した判断と、採用しなかった案の理由

追記のみ。書き換えない。

## 2026-08-31 allowedTools を絞るときは「使えないツール」をプロンプトに明記する

**問題**: 2026-08-31 の定期実行で、判定ステップが `is_error: false`・14ターン・658秒・$1.13 を
使いながら `out/judgments.json` を書かずに終了し、全191件が未判定で配信された。

**原因**: モデルは191件のJSONを加工するのに **`python3 -c ...` を Bash で実行しようとしていた**。
`--allowedTools "Read,Write"` なので拒否され（`permission_denials_count: 6`）、
代替手段に切り替える前に手数を使い切っていた。再現時は3回で諦めて Write に切り替わり成功したので、
**失敗は確率的**（試行回数がたまたま多いと成果物なしで終わる）。

**決定**: プロンプトに「使えるのは Read / Write / TodoWrite の3つだけ。Bash は使えない。
python3 / jq / node は試さないこと」と明記する。あわせて `TodoWrite` を許可して拒否ノイズを減らす。

**一般化（他の定時AIジョブにも効く）**: `--allowedTools` で絞るときは、**絞った事実を
プロンプト側にも書く**こと。モデルは許可リストを知らないので、無いツールを探して手数を
消耗し、無言で成果物なしに終わることがある。許可リストは「防御」、プロンプトの明記は「案内」で、
両方要る。

**採用しなかった案**: Bash を許可する。RSSの見出し・概要は第三者が書いた未検証テキストなので、
インジェクションが通ったときに任意コマンドが動く経路を作りたくない。

**次に再発したら**: `id`（16桁）ではなく配列の添字で判定を返させる案を検討する
（出力量が3割減り、ID転記ミスの経路も消える）。判定ステップのリトライ追加も選択肢。

## 2026-08-30 cron の分を `:00` から外す（05:00 → 05:11 JST）

**決定**: `schedule` を `0 20 * * *` から `11 20 * * *`（UTC）に変更する。

**理由**: GitHub の schedule は実行時刻を保証しない。実測で **2026-08-28 20:00Z 予定の回が
6時間29分遅れて 2026-08-29 02:29Z に実行**され、配信が 11:29 JST になった。翌日も
20:00Z 予定に対し 21:14Z 時点で未実行・キューにも入っていなかった。
毎時0分はリクエストが集中する枠なので、そこから外して遅延の縮小を狙う。

**制約**: **05:00 JST より前に動かしてはいけない**。24時間窓は「直近に閉じた 05:00 境界」で
切るので、05:00 前に走ると1日前の窓になり、既出除外でほぼ空の配信になる。
変更するなら 05:00〜06:00 JST（UTC 20:00〜21:00）の範囲に置くこと。

**効果は保証されない**。改善しなければ、時刻の正確さが要る運用ではないと割り切るか、
Mac mini の launchd に移す（ただしMacの起動が前提になりActionsの利点を失う）。

**データは落ちない**: 窓が固定境界なので、何時間遅れても対象記事は変わらない。
遅れるのは届く時刻だけ。この設計の効果が実地で確認できた形。

## 2026-08-28 実行基盤は GitHub Actions の cron。gh-aw は使わない

**決定**: `anthropics/claude-code-action@v1` を GitHub Actions の `schedule` から呼ぶ。

**採用しなかった案と理由**:

- **gh-aw（GitHub Agentic Workflows）× Copilot**: `202608-gh-aw-demo` で3回実行してすべて失敗。
  `copilot-requests: write` は org の Copilot サブスクリプションと集中課金が前提で、個人アカウント
  （groundcobra009）配下では推論に到達できない。`400 The requested model is not supported` は
  モデル名の問題ではなく推論トークンの権限不足のサイン（`auto` でも `gpt-5` でも同じ結果だったのが根拠）。
- **Claude Code クラウド版の scheduled agent**: Actions と比べて Secrets 管理と失敗通知の仕組みが弱く、
  送信失敗に気づきにくい。毎朝の配信という「失敗を検知できることが最重要」の用途に向かない。

## 2026-08-28 エージェントに任せるのは judgments.json の書き出しだけ

**決定**: 収集・整形・レンダー・送信はすべて決定論的な Node スクリプトが行い、LLM は
`out/judgments.json` を1つ書くだけ。判定が無い／壊れていても `src/judgments.js` の契約層が
「未判定」に倒し、メールは必ず届く。

**理由**: 隣の案件 `miraichi-20250803-github-actions-info-collector-demo` の毎朝配信は
`CLAUDE_CODE_OAUTH_TOKEN` 未登録のまま **1ヶ月間毎日 failure** し、誰も気づかなかった。
エージェントに全工程を任せると、失敗が無言になる。
また、決定論的な部分をスクリプトに寄せることで TDD が成立する（エージェント実行はテストできない）。

**副次的な効果**: プロンプトインジェクションが仮に通っても、書けるのは judgments.json だけで、
それは契約層が検証する（`allowedTools` は `Read,Write` のみ・Bash と WebFetch は与えない）。

## 2026-08-28 LLM認証は CLAUDE_CODE_OAUTH_TOKEN（APIキー従量課金にしない）

**決定**: Max200 契約を使う OAuth トークン方式。AI費用の追加課金はゼロ。

**採用しなかった案**: `ANTHROPIC_API_KEY`（`japan-news-frontpage-index` で実績あり・従量課金）。
月数百円程度だが、すでにサブスクリプションがあるため二重払いになる。

**制約**: `claude setup-token` は対話認可が必須で AI社員は実行できない。本人がターミナルで1回実行し、
`gh secret set` で登録する必要がある（有効期限1年）。

## 2026-08-28 24時間窓は JST 5:00 固定境界にする

**決定**: 窓は `[前日05:00 JST, 当日05:00 JST)`。実行時刻から24時間を引く方式は採らない。

**理由**: GitHub の `schedule` は混雑時に数分〜数十分遅れる（miraichi の解説資料に実測メモあり）。
実行時刻基準だと遅延のたびに窓がずれ、記事の取りこぼし（穴）と重複が発生する。
固定境界ならどれだけ遅れても穴が出ない。境界に落ちた記事は翌日分に回る。

## 2026-08-28 キーワードは「捨てる」ためではなく「並べる」ために使う

**決定**: `config/keywords.yaml` にマッチしない記事も候補に残す。キーワードはスコアの
加点・減点のみに使い、上限（既定250件）まではスコア順に全部LLMへ渡す。

**理由**: bid-radar はキーワードを通過ゲートとして使っているが、ニュースダイジェストで同じことを
すると「キーワード表になかった想定外の大ニュース」を構造的に取りこぼす。
日付が取れない記事も同じ理由で捨てない（重複配信は `processed-items.json` で防げる）。

## 2026-08-28 自分宛の自動配信は承認キューの対象外とする

**決定**: 送信先が**本人のGmail 1件のみ**である限り、承認キュー（ai-company CLAUDE.md §2）を
経由せずに GitHub Actions から自動送信してよい。

**理由**: 外部（第三者）への影響がなく、`japan-news-frontpage-index`・
`miraichi-20250803-github-actions-info-collector` の両案件が同じ構成で運用されている前例がある。

**この決定が無効になる条件（そのときは承認キューに戻す）**:

- 宛先に本人以外を1件でも追加するとき
- レポートを公開URL（GitHub Pages 等）で外部に出すとき
- 配信内容に顧客情報・機密が入りうるようになったとき

なお `AGENTS.md` の「メール送信は絶対にしない」は**AIエージェントが手で送ること**の禁止であり、
本リポジトリで本人が構築・承認した自動配信パイプラインはこの決定が優先する。
